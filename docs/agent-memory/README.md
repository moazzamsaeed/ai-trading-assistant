# Agent memory (backup)

Mirror of the Claude Code agent memory for this project
(`~/.claude/projects/.../memory/`), committed here so the strategy
decisions / paper-trading record survive a NUC failure.

`MEMORY.md` is the index; each `project_*.md` / `reference_*.md` / `user_*.md`
is one fact/record. These are notes, NOT runtime code — the bot does not read
them. Re-sync by re-copying the memory dir into this folder.

Last synced: 2026-06-26 (equities scanner + condor v2 first win).
